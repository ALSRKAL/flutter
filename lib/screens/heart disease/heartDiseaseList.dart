import 'package:flutter/material.dart';

class heartDiseaseList extends StatelessWidget {
  const heartDiseaseList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}